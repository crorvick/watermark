bin_PROGRAMS = watermark

watermark_SOURCES = main.c debug.c debug.h
watermark_CPPFLAGS = $(CAIRO_CFLAGS) $(GDK_CFLAGS) $(GDK_PIXBUF_CFLAGS)
watermark_LDADD = $(CAIRO_LIBS) $(GDK_LIBS) $(GDK_PIXBUF_LIBS)
